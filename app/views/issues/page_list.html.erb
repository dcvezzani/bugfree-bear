<h1>All pages</h1>

<ul>
<% @issues.each do |issue| %>
  <li>
    <% link_to "#{issue.title}.html", issue_path(issue) %>
    <%= link_to "wget -P unprocessed #{issue_page_url(issue.title)}", issue_page_url(issue.title) %>
  </li>
<% end %>
</ul>

